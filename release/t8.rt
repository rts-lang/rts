
# todo
# работает, но есть ошибка с параметрами
# кроме того проблема с указанием типов данных параметров?
ab(b) -> Int
 # здесь ожидается структура с любым уровнем модификации,
 # они будут работать в зависимости от контекста метода и
 # не приведут к ошибке в любом случае
 println(f"b -> {b}")
 = b -10

a = ab(1)
println(a)
println(type(a))
println(mut(a))

c
 10+20
 20
println(c.0,c.1)
