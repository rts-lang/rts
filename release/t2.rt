
# Видно, что вложенные структуры ab, b приведут к ссылкам и они будут работать;
  Стоит помнить, что ссылкой не является только одно слово, поэтому ab() был бы методом;
a
  ab(a:UInt, b:Int) -> UInt
    = a+b
  b
    println(10)
c = a.ab(10,-20)
print(c)
a.b()

# Однако, если пересвоить значение, то мы можем получить ссылку в виде одного слова;
p
  println(111)
d = p

println(f"{d}:{type(d)}")
d()
d()